language: node_js
node_js:
  - "10.14.2"

cache:
  directories:
  - $HOME/.npm

notifications:
  email:
    on_failure: change
    on_success: change

install:
  - npm ci

script:
  - npm run lint
  - npm run test
  - npm run build:fosdem19:ssr:production
  - sh build-docker-fosdem19.sh
